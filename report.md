# Technical Report: High-Precision Attendance System with Multi-Scale Anti-Spoofing

**Project:** AI Smart Kiosk for Student Attendance
**Version:** 3.0 (Optimized Pipeline & Advanced Liveness)
**Date:** March 04, 2026

---

## 1. Abstract

Báo cáo trình bày chi tiết về hệ thống điểm danh khuôn mặt thời gian thực, kết hợp các công nghệ SOTA (**ArcFace**, **Qdrant**, **Mediapipe**) và cơ chế bảo mật đa tầng. Hệ thống V3.0 tập trung vào việc cân bằng giữa tốc độ xử lý "tức thì" (nhờ **CPU Affine Alignment**) và khả năng chống giả mạo thụ động mạnh mẽ (**Multi-Scale FAS**), đảm bảo an toàn trước các phương thức tấn công bằng hình ảnh và video quay lại (Replay Attacks).

---

## 2. Kiến trúc Hệ thống (System Architecture)

Hệ thống được xây dựng theo mô hình **Modular Architecture**, chia thành các khối chức năng độc lập:

- **Frontend (Web-based):** HTML/JS giao diện phẳng, hiển thị feedback màu sắc thời gian thực.
- **Application Logic (`app.py`):** Quản lý luồng Camera, xử lý Mediapipe Face Mesh và điều phối các Thread nhận diện.
- **Liveness Engine (`anti_spoof.py`):** Thực thi mô hình MiniFASNetV2 với chiến lược phân tích đa quy mô.
- **Recognition Engine (`recognition.py`):** Trích xuất Embedding ArcFace và truy vấn Vector DB.
- **Database Layer (`kiosk_db.py`):** Kết nối Qdrant (Vector) và SQLite (Metadata).

---

## 3. Các Giải pháp Công nghệ & Cải tiến

### 3.1. Pipeline Nhận diện Tối ưu

### 3.1. Pipeline Nhận diện SOTA (V3.5)

Mỗi khung hình đi qua một pipeline tinh gọn nhưng đạt độ chính xác cực cao:

1. **Detection (Mediapipe):** Tìm 468 điểm landmarks nhanh chóng trên CPU.
2. **Standard 5-Point Alignment:** Sử dụng 5 điểm landmarks (mắt, mũi, miệng) để map chính xác vào template **112x112** tiêu chuẩn của ArcFace.
   - *Ưu điểm:* Đưa khuôn mặt về đúng "vùng hiểu biết" tốt nhất của AI, tối ưu hóa sai số do góc nghiêng.
3. **Test-Time Augmentation (TTA):** Đối với mỗi lần nhận diện, hệ thống không chỉ lấy 1 embedding đơn lẻ mà tạo ra **3 biến thể** của khuôn mặt (Gốc, Cân bằng sáng CLAHE, Tăng cường độ tương phản).
   - *SOTA Technique:* Hệ thống tính toán **Trung bình cộng của 3 vector embedding** để tạo ra một "Robust Embedding" cuối cùng, loại bỏ hoàn toàn nhiễu từ ánh sáng và camera.

### 3.2. Chống giả mạo Đa quy mô (Multi-Scale FAS)

Đây là cải tiến quan trọng nhất trong V3.0 để chặn video replay mà không cần người dùng hành động:

- **Scale 1 (Tight Crop - 1.0x):** Tập trung vào kết cấu da (Skin Texture).
- **Scale 2 (Wide Crop - 2.7x):** Nhìn rộng ra bối cảnh xung quanh để phát hiện các dấu hiệu vật lý của thiết bị giả mạo như: viền màn hình điện thoại, ánh sáng phản xạ từ mặt kính, hoặc sự mất chiều sâu so với hậu cảnh.
- **Logic thắt chặt (The MIN Rule):**
  `Final_Score = min(Score_Tight, Score_Wide)`
  Chỉ cần 1 trong 2 quy mô có dấu hiệu bất thường, hệ thống sẽ đánh dấu là **Fake**.
- **Blink Detection:** Lớp bảo vệ bổ sung bằng cách tính toán EAR (Eye Aspect Ratio).

### 3.3. Logic Xác nhận Thông minh (Decision Making)

Hệ thống không đánh cược vào 1 frame duy nhất mà sử dụng cơ chế bình chọn:

- **Fast Pass:** Nếu độ tương đồng > 0.75, xác nhận ngay.
- **Top-5 Voting:** Truy vấn 5 kết quả gần nhất trong Qdrant. Chỉ sinh viên đạt đa số phiếu bầu mới được ghi nhận.
- **Ambiguity Gap:** Nếu chênh lệch giữa sinh viên Top 1 và Top 2 quá nhỏ (< 0.05), hệ thống sẽ từ chối để tránh nhận nhầm giữa những người có nét mặt giống nhau.

### 3.4. Kiểm soát độ ổn định (Stability Logic)

Để tránh "nhận diện vụt" (nhận diện sai khi người dùng đang di chuyển), hệ thống áp dụng:

- **Face Stability:** Khuôn mặt phải giữ vị trí trong khung hình với sai số < 25px trong vòng **0.8 giây**.
- **Head Pose Check:** Sử dụng tỉ lệ khoảng cách mũi và mắt để đảm bảo sinh viên đang nhìn thẳng vào Camera.

---

## 4. Thiết kế Trải nghiệm (UI/UX)

Hệ thống sử dụng phản hồi màu sắc tối giản (2px thickness):

- **TRẮNG:** Đang tìm kiếm hoặc đối tượng ở quá xa (Face width < 180px).
- **CAM:** Đã vào vị trí quét, đang âm thầm kiểm tra Liveness.
- **XANH LÁ:** Xác nhận danh tính thành công.

---

## 5. Hiệu năng Thực tế

- **Tốc độ:** Nhận diện hoàn tất trong < 1 giây kể từ khi sinh viên đứng yên.
- **Độ chính xác:** Giảm tỉ lệ False Acceptance Rate (FAR) xuống mức tối thiểu nhờ logic Multi-Scale.
- **Khả năng mở rộng:** Nhờ Qdrant HNSW Index, tốc độ tìm kiếm không bị suy giảm khi số lượng sinh viên tăng lên hàng chục nghìn.

---

## 6. Kết luận

Hệ thống Smart Kiosk V3.0 là sự kết hợp hoàn hảo giữa thuật toán AI hiện đại và các kỹ thuật xử lý ảnh CPU truyền thống. Việc chuyển dịch từ nhận diện "chủ động" (bắt người dùng thao tác) sang "thụ động" (AI tự phân tích bối cảnh) giúp tăng năng suất điểm danh tại trường học mà vẫn duy trì được hàng rào bảo mật vững chắc.

---
*Report Generated by Project Team - March 2026*
